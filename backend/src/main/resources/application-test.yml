# ===================================================================
# EVShare 3D Platform - Test Profile ('test')
# ===================================================================

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: WARN
    com.example.evshare: INFO
